package com.neo.properties.util

object Instructions {

    const val INSTALL = "Use '$ ./properties install' to install"
    const val SAVE = "Use '$ properties save <tag>' to save the current environment"
}